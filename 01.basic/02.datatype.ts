// primitive data type
//alert (3+3) // cant use because we use node.js not browser.
//console.log(alert);
let name1 ="ali"  //string
let num = 1223    // number
let login  = true // boolean
let null1 = null  // its a  special data type which me empty but not 0
let symbol = Symbol // used for unique for data
let object ={}     // make a object 
let a =undefined
console.log(typeof(null)); // object 

